[providers]
image_editor_model = "gpt-image-2"
evaluator_model = "gpt-5.6"
timeout_seconds = 120

[limits]
max_image_size_mb = 10

[logging]
level = "WARNING"

[storage]
artifact_root = "runtime/test-tasks" # Isolated storage for a deployed test instance
